@{
    Id          = "c3"
    DisplayName = "AC03-Require-Trusted-Location"
    Description = "Step-up authentication: action requires the user to be on a trusted internal network location."
    IsAvailable = $true
}
